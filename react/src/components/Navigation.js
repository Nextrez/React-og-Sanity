import { NavLink } from "react-router-dom";

const Navigation = () => (
    <nav>
        <ul>
            <li><NavLink to="/">Hjem</NavLink></li>
        </ul>
    </nav>
);

export default Navigation;